# Test configuration overrides
# Use shared in-memory SQLite so multiple connections see the same DB
# See https://www.sqlite.org/inmemorydb.html

db.url=jdbc:sqlite:file:unitdb?mode=memory&cache=shared
